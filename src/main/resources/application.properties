# ─── Application ──────────────────────────────────────────────────────────────
spring.application.name=complaint-management-system
server.port=8080

# ─── Database — H2 FILE-BASED (data persists across restarts) ─────────────────
#
#  The database is stored in the data/ folder at the project root.
#  Files created automatically on first run:
#    data/cmsdb.mv.db      ← all tables and rows live here
#    data/cmsdb.trace.db   ← query trace log (optional)
#
#  DB_CLOSE_DELAY=-1  : keeps the connection open as long as the JVM is running
#  DB_CLOSE_ON_EXIT=false : does not close the DB file when the JVM shuts down
#                           so data is safely flushed and readable next startup
#
spring.datasource.url=jdbc:h2:file:./data/cmsdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Console — open http://localhost:8080/h2-console to browse tables
# IMPORTANT: when connecting, use JDBC URL: jdbc:h2:file:./data/cmsdb
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=false

# ─── JPA / Hibernate ──────────────────────────────────────────────────────────
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# 'update' — creates tables on first run, then only applies schema changes.
# Never drops existing data. Safe for persistent file-based H2.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ─── Logging ──────────────────────────────────────────────────────────────────
logging.level.com.cms=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n

# ─── Jackson ──────────────────────────────────────────────────────────────────
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss

# ─── Actuator ─────────────────────────────────────────────────────────────────
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# ─── Custom app info ──────────────────────────────────────────────────────────
info.app.name=Complaint Management System
info.app.version=1.0.0
info.app.description=REST API for managing customer complaints

# ─── Switch to MySQL (uncomment block below, comment out H2 block above) ──────
# spring.datasource.url=jdbc:mysql://localhost:3306/cmsdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=yourpassword
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
# spring.jpa.hibernate.ddl-auto=update
# spring.h2.console.enabled=false
